#ifndef HWFEATURES_H
#define HWFEATURES_H

#include <sysFeatures_types.h>
#include <sysFeatures_list.h>

#define HWFEATURES_MAX_STR_LENGTH 100
#define HWFEATURES_MIN_STRLEN(a, b) ((a) > (b) ? (b) : (a))


#endif
