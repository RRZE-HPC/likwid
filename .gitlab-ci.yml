

build-x86-daemon:
    stage: build
    variables:
      LIKWID_COMPILER: "GCC"
      LIKWID_ACCESSMODE: "accessdaemon"
      LIKWID_MODULE: "likwid/5.2-dev"
    script:
      - mkdir -p /tmp/build-x86-daemon
      - module load "$LIKWID_MODULE"
      - export LIKWID_PREFIX=$(realpath $(dirname $(which likwid-topology))/..)
      - module unload "$LIKWID_MODULE"
      - sed -e s+"ACCESSMODE = .*"+"ACCESSMODE=$LIKWID_ACCESSMODE"+g -i config.mk
      - sed -e s+"COMPILER = .*"+"COMPILER=$LIKWID_COMPILER"+g -i config.mk
      - sed -e s+"PREFIX ?= .*"+"PREFIX=/tmp/build-x86-daemon"+g -i config.mk
      - sed -e s+"INSTRUMENT_BENCH = .*"+"INSTRUMENT_BENCH=true"+g -i config.mk
      - sed -e s+"INSTALLED_ACCESSDAEMON = .*"+"INSTALLED_ACCESSDAEMON=$LIKWID_PREFIX/sbin/likwid-accessD"+g -i config.mk
      - sed -e s+"BUILDDAEMON = .*"+"BUILDDAEMON=false"+g -i config.mk
      - sed -e s+"BUILDFREQ = .*"+"BUILDFREQ=false"+g -i config.mk
      - cat config.mk
      - make
      - make BUILDDAEMON=false BUILDFREQ=false install
      - cp -r /tmp/build-x86-daemon $CI_PROJECT_DIR/build-x86-daemon
      - if [ -e build-x86-daemon/sbin/likwid-accessD ]; then rm build-x86-daemon/sbin/likwid-accessD; fi
      - if [ -e build-x86-daemon/sbin/likwid-setFreq ]; then rm build-x86-daemon/sbin/likwid-setFreq; fi
      - rm -rf /tmp/build-x86-daemon
    artifacts:
      paths:
        - build-x86-daemon/
    tags:
      - testcluster


build-x86-perf:
    stage: build
    variables:
      LIKWID_COMPILER: "GCC"
      LIKWID_ACCESSMODE: "perf_event"
    script:
      - mkdir /tmp/build-x86-perf
      - sed -e s+"ACCESSMODE = .*"+"ACCESSMODE=$LIKWID_ACCESSMODE"+g -i config.mk
      - sed -e s+"COMPILER = .*"+"COMPILER=$LIKWID_COMPILER"+g -i config.mk
      - sed -e s+"PREFIX ?= .*"+"PREFIX=/tmp/build-x86-perf"+g -i config.mk
      - sed -e s+"INSTRUMENT_BENCH = .*"+"INSTRUMENT_BENCH=true"+g -i config.mk
      - sed -e s+"BUILDDAEMON = .*"+"BUILDDAEMON=false"+g -i config.mk
      - sed -e s+"BUILDFREQ = .*"+"BUILDFREQ=false"+g -i config.mk
      - make
      - make BUILDDAEMON=false BUILDFREQ=false install
      - cp -r /tmp/build-x86-perf $CI_PROJECT_DIR/build-x86-perf
      - if [ -e build-x86-perf/sbin/likwid-accessD ]; then rm build-x86-perf/sbin/likwid-accessD; fi
      - if [ -e build-x86-perf/sbin/likwid-setFreq ]; then rm build-x86-perf/sbin/likwid-setFreq; fi
      - rm -rf /tmp/build-x86-perf
    artifacts:
      paths:
        - build-x86-perf/
    tags:
      - testcluster

build-x86-daemon-nv:
    stage: build
    variables:
      LIKWID_COMPILER: "GCC"
      LIKWID_ACCESSMODE: "accessdaemon"
      LIKWID_MODULE: "likwid/5.2-dev"
      CUDA_MODULE: "cuda/10.2"
      SLURM_NODELIST: medusa
    script:
      - mkdir -p /tmp/build-x86-daemon-nv
      - module load "$LIKWID_MODULE"
      - module load "$CUDA_MODULE"
      - export LIKWID_PREFIX=$(realpath $(dirname $(which likwid-topology))/..)
      - sed -e s+"ACCESSMODE = .*"+"ACCESSMODE=$LIKWID_ACCESSMODE"+g -i config.mk
      - sed -e s+"COMPILER = .*"+"COMPILER=$LIKWID_COMPILER"+g -i config.mk
      - sed -e s+"PREFIX ?= .*"+"PREFIX=/tmp/build-x86-daemon-nv"+g -i config.mk
      - sed -e s+"INSTRUMENT_BENCH = .*"+"INSTRUMENT_BENCH=true"+g -i config.mk
      - sed -e s+"NVIDIA_INTERFACE = .*"+"NVIDIA_INTERFACE=true"+g -i config.mk
      - sed -e s+"INSTALLED_ACCESSDAEMON = .*"+"INSTALLED_ACCESSDAEMON=$(echo "$LIKWID_PREFIX")/sbin/likwid-accessD"+g -i config.mk
      - sed -e s+"BUILDDAEMON = .*"+"BUILDDAEMON=false"+g -i config.mk
      - sed -e s+"BUILDFREQ = .*"+"BUILDFREQ=false"+g -i config.mk
      - make
      - make BUILDDAEMON=false BUILDFREQ=false install
      - cp -r /tmp/build-x86-daemon-nv $CI_PROJECT_DIR/build-x86-daemon-nv
      - if [ -e build-x86-daemon-nv/sbin/likwid-accessD ]; then rm build-x86-daemon-nv/sbin/likwid-accessD; fi
      - if [ -e build-x86-daemon-nv/sbin/likwid-setFreq ]; then rm build-x86-daemon-nv/sbin/likwid-setFreq; fi
      - rm -rf /tmp/build-x86-daemon-nv
    artifacts:
      paths:
        - build-x86-daemon-nv/
    tags:
      - testcluster

build-x86-perf-nv:
    stage: build
    variables:
      LIKWID_COMPILER: "GCC"
      LIKWID_ACCESSMODE: "perf_event"
      SLURM_NODELIST: medusa
      CUDA_MODULE: "cuda/10.2"
    script:
      - mkdir -p /tmp/$CI_JOB_NAME
      - module load "$CUDA_MODULE"
      - sed -e s+"ACCESSMODE = .*"+"ACCESSMODE=$(echo "$LIKWID_ACCESSMODE")"+g -i config.mk
      - sed -e s+"COMPILER = .*"+"COMPILER=$(echo "$LIKWID_COMPILER")"+g -i config.mk
      - sed -e s+"PREFIX ?= .*"+"PREFIX=/tmp/$CI_JOB_NAME"+g -i config.mk
      - sed -e s+"INSTRUMENT_BENCH = .*"+"INSTRUMENT_BENCH=true"+g -i config.mk
      - sed -e s+"NVIDIA_INTERFACE = .*"+"NVIDIA_INTERFACE=true"+g -i config.mk
      - sed -e s+"BUILDDAEMON = .*"+"BUILDDAEMON=false"+g -i config.mk
      - sed -e s+"BUILDFREQ = .*"+"BUILDFREQ=false"+g -i config.mk
      - make
      - make BUILDDAEMON=false BUILDFREQ=false install
      - cp -r /tmp/$CI_JOB_NAME $CI_PROJECT_DIR/$CI_JOB_NAME
      - if [ -e $CI_JOB_NAME/sbin/likwid-accessD ]; then rm $CI_JOB_NAME/sbin/likwid-accessD; fi
      - if [ -e $CI_JOB_NAME/sbin/likwid-setFreq ]; then rm $CI_JOB_NAME/sbin/likwid-setFreq; fi
      - rm -rf /tmp/$CI_JOB_NAME
    artifacts:
      paths:
        - $CI_JOB_NAME/
    tags:
      - testcluster

build-arm8-perf:
    stage: build
    variables:
      LIKWID_COMPILER: "GCCARMv8"
      LIKWID_ACCESSMODE: "perf_event"
      SLURM_NODELIST: warmup
    script:
      - mkdir /tmp/build-arm8-perf
      - sed -e s+"ACCESSMODE = .*"+"ACCESSMODE=$LIKWID_ACCESSMODE"+g -i config.mk
      - sed -e s+"COMPILER = .*"+"COMPILER=$LIKWID_COMPILER"+g -i config.mk
      - sed -e s+"PREFIX ?= .*"+"PREFIX=/tmp/build-arm8-perf"+g -i config.mk
      - sed -e s+"INSTRUMENT_BENCH = .*"+"INSTRUMENT_BENCH=true"+g -i config.mk
      - sed -e s+"BUILDDAEMON = .*"+"BUILDDAEMON=false"+g -i config.mk
      - sed -e s+"BUILDFREQ = .*"+"BUILDFREQ=false"+g -i config.mk
      - make
      - make BUILDDAEMON=false BUILDFREQ=false install
      - cp -r /tmp/build-x86-perf $CI_PROJECT_DIR/build-arm8-perf
      - if [ -e build-arm8-perf/sbin/likwid-accessD ]; then rm build-arm8-perf/sbin/likwid-accessD; fi
      - if [ -e build-arm8-perf/sbin/likwid-setFreq ]; then rm build-arm8-perf/sbin/likwid-setFreq; fi
      - rm -rf /tmp/build-arm8-perf
    artifacts:
      paths:
        - build-arm8-perf/
    tags:
      - testcluster


check-event-files:
    stage: test
    tags:
      - testcluster
    script:
      - module load python
      - test/check_data_files.py events
#      - test/check_data_files.py groups

generator:
  stage: build
  tags:
    - testcluster
  variables:
    NO_SLURM_SUBMIT: 1
  script: 
   - test/generate_ci_jobs.sh > generated-pipeline.yml
  artifacts:
    paths:
      - generated-pipeline.yml

testcluster-pipeline:
  stage: test
  trigger:
    include:
      - artifact: generated-pipeline.yml
        job: generator
    strategy: depend
  variables:
    PARENT_PIPELINE_ID: $CI_PIPELINE_ID

